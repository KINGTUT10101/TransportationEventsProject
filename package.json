{
  "name": "transportationeventsproject",
  "version": "1.0.0",
  "description": "The final project for Database Theory and Design",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "server": "node --watch --max-old-space-size=4096 ./server/index.js",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm run build --prefix client"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "xml2js": "^0.6.2"
  }
}
